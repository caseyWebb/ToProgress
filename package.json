{
  "name": "toprogress2",
  "version": "2.0.0",
  "description": "A lightweight, no-dependency top progress bar library",
  "main": "dist/umd/index.js",
  "module": "dist/es/index.js",
  "typings": "dist/es/index.d.ts",
  "scripts": {
    "build": "yarn run build:es && yarn run build:umd",
    "build:es": "tsc --declaration --declarationMap --module es2015 --outDir dist/es",
    "build:umd": "tsc --module umd --outDir dist/umd",
    "build:docs": "typedoc --disableOutputCheck --mode file --theme minimal --out docs --excludePrivate --excludeNotExported index.ts",
    "build:demo": "parcel build --out-dir docs/demo --public-url /toprogress2/demo demo/index.html",
    "lint": "tsc --noEmit && tslint -p ./",
    "prepush": "yarn lint",
    "prepublishOnly": "yarn test && yarn build",
    "start:demo": "parcel demo/index.html",
    "test": "jest"
  },
  "keywords": [
    "topbar",
    "progress",
    "progressbar"
  ],
  "author": "Randy Lu <randypriv@gmail.com>",
  "contributors": [
    "Casey Webb <notcaseywebb@gmail.com>"
  ],
  "license": "MIT",
  "repository": "caseyWebb/ToProgress",
  "bugs": "https://github.com/caseyWebb/ToProgress/issues",
  "homepage": "https://github.com/caseyWebb/ToProgress#readme",
  "devDependencies": {
    "@types/jest": "^22.0.1",
    "husky": "^0.14.3",
    "jest": "^22.0.6",
    "parcel-bundler": "^1.4.1",
    "ts-jest": "^22.0.1",
    "tslint": "^5.9.1",
    "typedoc": "^0.9.0",
    "typescript": "^2.6.2"
  },
  "jest": {
    "collectCoverage": true,
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ],
    "coverageReporters": [
      "lcov",
      "html"
    ],
    "moduleFileExtensions": [
      "js",
      "ts"
    ],
    "testRegex": ".*test\\.ts?$",
    "transform": {
      "^.+\\.[tj]sx?$": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    }
  }
}
